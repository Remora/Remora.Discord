<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFrameworks>netstandard2.1;net5.0</TargetFrameworks>
    </PropertyGroup>

    <PropertyGroup>
        <Version>12.0.0</Version>
        <Description>
            $(BaseDescription)

            This package contains the default implementations of Discord's REST API, complete with client-side sanity
            checks and rate limiting support.
        </Description>
        <PackageReleaseNotes>
            Update user flags.
            Update pin/unpin method names.
            Restructure application layout.
            Update GetCurrentUserGuildsAsync limits.
            Fix unit test.
            Update member nullability.
            Fix missing parameter.
            Update emoji format support.
            Correct nullability of member.
            Add a bunch of error codes.
            Add Team.Name.
            Add Message.ApplicationID.
            Implement stage instances.
            Rename thread endpoints in accordance with Discord docs.
        </PackageReleaseNotes>
    </PropertyGroup>

    <ItemGroup>
      <PackageReference Include="Microsoft.Extensions.Http.Polly" Version="5.0.1" />
      <PackageReference Include="Polly.Contrib.WaitAndRetry" Version="1.1.1" />
    </ItemGroup>

    <ItemGroup>
      <ProjectReference Include="..\Remora.Discord.API\Remora.Discord.API.csproj" />
    </ItemGroup>

</Project>
